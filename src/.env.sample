# Azure Content Understanding - Foundry resource (region where CU is available)
CONTENTUNDERSTANDING_ENDPOINT=https://<cu-resource-name>.services.ai.azure.com/

# Foundry project where model deployments live (can be a different region/resource)
MODELS_ENDPOINT=https://<models-resource-name>.services.ai.azure.com/api/projects/<project-name>

# Optional: API key. If omitted, DefaultAzureCredential is used (az login).
# CONTENTUNDERSTANDING_KEY=

# Model deployment names (must match what you deployed at MODELS_ENDPOINT)
GPT_4_1_DEPLOYMENT=gpt-4.1
GPT_4_1_MINI_DEPLOYMENT=gpt-4.1-mini
TEXT_EMBEDDING_3_SMALL_DEPLOYMENT=text-embedding-3-small
